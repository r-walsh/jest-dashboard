{
  "name": "jest-dashboard",
  "version": "1.0.0-beta",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prettier": "prettier --single-quote --trailing-comma all --write \"./index.js\" \"./src/**/*.js\"",
    "test": "jest",
    "test:watch": "npm run test -- --watch"
  },
  "jest": {
    "reporters": [
      "./testing.js"
    ]
  },
  "keywords": [
    "jest",
    "dashboard"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^20.0.4",
    "prettier": "^1.5.3"
  },
  "dependencies": {
    "blessed": "^0.1.81",
    "cross-spawn": "^5.1.0",
    "jest-cli": "^20.0.4"
  }
}
